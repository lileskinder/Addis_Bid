class BidBundle {
  final int gapPrice, startingPrice;
  final String productName, productCat, imageSrc, timeStamp;

  BidBundle(this.imageSrc, this.productName, this.productCat, this.gapPrice,
      this.startingPrice, this.timeStamp);
}
